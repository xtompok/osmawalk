-------some of these packages might be unnecessary ------
sudo apt-get install build-essential libxml2-dev libgeos++-dev libpq-dev libbz2-dev libtool automake curl zip unzip
---------------------------------------------------------

sudo apt-get install postgresql postgis postgresql-9.3-postgis-2.1
sudo apt-get install libprotobuf-c0-dev protobuf-c-compiler libprotobuf-dev protobuf-compiler 
sudo apt-get install libproj-dev libyaml-dev 
#sudo apt install python-yaml python-pyproj python-protobuf python-imposm-parser python-networkx

===== Prepare external libraries =====

-------get libucw -------

    1, wget http://www.ucw.cz/libucw/download/libucw-6.5.tar.gz
    2, extract
    3, ./configure
    # apt install debhelper fakeroot <- debian scripts does not work
    4, make
    5, sudo make install

-------get OSM-binary-------
    1, cd ext-lib
    2, git clone http://github.com/scrosby/OSM-binary.git

-------get libosm-------
    1, cd ext-lib
    2, git clone https://github.com/xtompok/libosmpbf
    3, cd libosmpbf
    4, make # Will fail, but libosm.so is built and it is the only needed component

-------get libcsv-------
    1, apt install libcsv3 libcsv-dev

-------get libraptor-----
    1, cd ext-lib
    2, git clone https://github.com/xtompok/mmpf
    3, cd mmpf/raptor
    4, make

===== Compile the project ======
    cd osm 
    make
    cd ..

    cd compiled
    make
    cd ..

-------Prepare data for Czech Republic-------
    cd osm
    make
    ./prepare.sh

------Prepare database------

    1, Create database "osmwalk-prepare"
    2, Give your user access <db_username> rights to the "osmwalk-prepare" database
    3, Initialize postgis: 
        CREATE EXTENSION postgis;

------- Preprocessing - give postgress as much memory, as possible------
	cd compiled
	./parse ../osm/praha.osm
	./todb.sh -u db_username

	cd ../postgis
	./process.sh -u db_username

	cd ../compiled
	./csvtograph ../data/
	cd ../data
	cp postgis-graph.pbf praha-graph.pbf

    use ./search or use web application in webapp/ folder
